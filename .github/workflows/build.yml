name: Simple Local

on: push

jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:18
    steps:
      - runs: node --version
      - runs: npm --version
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm install --save moment
      - run: node app.js